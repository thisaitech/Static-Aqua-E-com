-- ════════════════════════════════════════════════════════════════
-- 🔐 MAKE ME ADMIN - SIMPLEST METHOD
-- ════════════════════════════════════════════════════════════════
-- Copy and paste this ENTIRE script into Supabase SQL Editor
-- Then click RUN
-- ════════════════════════════════════════════════════════════════

-- STEP 1: See all your users
-- Look for YOUR email in the results below
-- ════════════════════════════════════════════════════════════════
SELECT
  id,
  email,
  created_at
FROM auth.users
ORDER BY created_at DESC;


-- ════════════════════════════════════════════════════════════════
-- STEP 2: Add the user as admin
-- ⚠️ CHANGE THE EMAIL BELOW to match YOUR email from Step 1
-- ════════════════════════════════════════════════════════════════
INSERT INTO admins (id, created_at)
SELECT id, NOW()
FROM auth.users
WHERE email = 'nanthini@thisaitech.com'  -- ⚠️⚠️⚠️ CHANGE THIS! ⚠️⚠️⚠️
ON CONFLICT (id) DO NOTHING;


-- ════════════════════════════════════════════════════════════════
-- STEP 3: Verify you're now an admin
-- You should see 1 row with your email
-- ════════════════════════════════════════════════════════════════
SELECT
  u.email,
  'I am now an admin!' as status
FROM admins a
JOIN auth.users u ON a.id = u.id
WHERE u.email = 'nanthini@thisaitech.com'  -- ⚠️⚠️⚠️ CHANGE THIS! ⚠️⚠️⚠️
LIMIT 1;


-- ════════════════════════════════════════════════════════════════
-- ✅ IF YOU SEE YOUR EMAIL ABOVE, YOU'RE DONE!
-- ════════════════════════════════════════════════════════════════
-- Now:
-- 1. Go back to your application
-- 2. Refresh the page (F5)
-- 3. Try generating the invoice again
-- 4. It should work! 🎉
-- ════════════════════════════════════════════════════════════════
